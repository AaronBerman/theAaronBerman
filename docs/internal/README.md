# Internal notes

This folder is for machine-local and private material: real emails, absolute paths, staging hosts, checklists that mention clients.

Do not copy these files into GitHub uploads, zips, or `docs/share/`.
They are gitignored except this README.

Put working notes in files next to this one on your machine (for example `local.md`).
