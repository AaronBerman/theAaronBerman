# Agent notes

## Layout

- `index.html` / `home.html` - GitHub Pages resume site
- `styles/` - theme CSS
- `resources/` - photo and resume files
- `CNAME` - theaaronberman.com

## Commands

Open `home.html` or serve the folder with any static file server. No build step.

## Rules

- Do not invent jobs, dates, or metrics. Resume copy must match the files in `resources/`.
- Do not commit `docs/internal/*` other than `docs/internal/README.md`.
- Keep the existing Apache 2.0 license. Do not switch it.
